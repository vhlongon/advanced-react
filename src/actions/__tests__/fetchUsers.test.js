import { FETCH_USERS } from '../types';
import { fetchUsers } from '../index';

describe('fetchUsers', () => {
  it('should return FETCH_USERS action', () => {
    //   const action = {
    //     type: FETCH_USERS,
    //     payload: []
    //   };
    //   expect(fetchUsers([])).toEqual(action);
    // TODO - test async action
    expect(true).toBe(true);
  });
});
